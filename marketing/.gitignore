# Generated by build.js — fetched from public.blog_posts at build time.
# Don't commit; the build runs on every Vercel deploy and on every
# Vercel Deploy Hook fire from publish-article (bylined_hosted path).
/blog/

# Generated by build.js at deploy time — regenerated every build.
/sitemap.xml
/robots.txt

# Node
node_modules/
