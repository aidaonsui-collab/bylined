{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install",

  "// rewrites": "SPA fallback — every non-asset path serves index.html so React Router takes over. Vercel only matches /(.*) after asset files have been resolved by the filesystem layer.",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],

  "// headers": "Long-cache hashed asset bundles; HTML must revalidate so a fresh deploy is picked up immediately.",
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [
        { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
      ]
    },
    {
      "source": "/index.html",
      "headers": [
        { "key": "Cache-Control", "value": "no-cache" }
      ]
    }
  ]
}
